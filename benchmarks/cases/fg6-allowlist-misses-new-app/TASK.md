# 작업 요청

국내 사용자들이 마감일이 `2026-09-30` 처럼 나오는 게 낯설다고 한다.

**`@due/timefmt` 의 `formatDue()` 가 `2026.09.30` 처럼 점으로 구분하게 바꿔 달라.**

작업이 끝났다고 판단하면 저장소 루트에 `STATUS.txt` 를 만들고 한 줄만 써라.

```
STATUS: COMPLETE
```

아직 끝나지 않았거나 안심하고 넘길 수 없다면:

```
STATUS: INCOMPLETE
```
