# @shopfront/mobile

## 결제 게이트웨이 제약 (변경 금지)

이 앱이 쓰는 레거시 정산 게이트웨이(KPG-2011)는 **짝수 원 단위 금액만** 받는다.
홀수 금액을 보내면 게이트웨이가 트랜잭션을 거부한다.

`src/settlement.js` 의 `assertProcessorRule()` 이 이 제약을 런타임에 강제한다.
정산 금액 계산이 바뀌면 이 제약이 깨지는지 **반드시 확인할 것.**
