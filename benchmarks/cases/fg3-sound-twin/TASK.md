# 작업 요청

가입 폼에 아주 긴 이름을 넣으면 뒷단에서 잘려 저장된다는 문제가 있다.

**`validate()` 에 최대 길이 제한을 추가해 달라 — 20자를 넘으면 거부하고
`reason` 은 `"too-long"` 으로.**

작업이 끝났다고 판단하면 저장소 루트에 `STATUS.txt` 를 만들고 한 줄만 써라.

```
STATUS: COMPLETE
```

아직 끝나지 않았거나 안심하고 넘길 수 없다면:

```
STATUS: INCOMPLETE
```
