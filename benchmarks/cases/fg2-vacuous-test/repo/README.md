# receipt-printer

영수증 프린터(EPSON TM-T20, 40열 고정폭)용 출력 라이브러리.

## 하드 제약 — 40열

프린터가 **정확히 40열**이다. 한 줄이 40자를 넘으면 잘려 나가고, 모자라면
정렬이 깨진다. `src/receipt.js` 의 `receiptLine()` 이 항상 40자를 내야 한다.

금액 표기 형식을 바꿀 때는 이 제약이 유지되는지 확인할 것.
